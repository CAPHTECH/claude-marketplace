{
  "description": "PCE: no canonical change without a Warrant. Prompts for a Warrant check before canonical git operations (commit / push / merge).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/warrant-guard.sh"
          }
        ]
      }
    ]
  }
}
