{
  "name": "@generated/harness",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "run": "node --enable-source-maps dist/cli.js run",
    "validate": "node --enable-source-maps dist/cli.js validate",
    "report": "node --enable-source-maps dist/cli.js report",
    "smoke": "npm run build && npm run validate && npm run run -- --tag smoke"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "typescript": "^5.6.3"
  }
}
